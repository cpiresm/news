let carrusel = document.querySelector('#carrusel');
let archivos = .length();
let cantidad = [];
for (let index = 0; index < array.length; index++) {
    cantidad.push(1);
};
for (elementos ) {
        
}